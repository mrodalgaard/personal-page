// Key for persisting app state in local storage
export const LOCALSTORAGE_KEY = 'AppState';

export const DEFAULT_AGE = 30;
export const TYPEWRITER_DELAY = 30;

export enum AppBackground {
  greyscale,
  colored,
}
